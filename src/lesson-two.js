//Write a calculator. Request 2 numbers and an operation as a symbol (+ - * /). The program solves it depending on the operation and shows the result.

// Count the factorial of the number given by a user. For example, 4! is 1 * 2 * 3 * 4 = 24, or 6! is 1 * 2 * 3 * 4 * 5 * 6 =  720.

/*let number = prompt("Enter a number to calculate the factorial of");
let total = 1;
for(i = number; i > 1; i--){
 total *= i;
}
console.log(total);*/


//Count sheep. Request a non-negative integer, 3 for example, and print a string with a murmur: "1 sheep...2 sheep...3 sheep..." to the page as a paragraph. If a user types a negative number in, alert an error.

/*let count = prompt('Enter a number of sheeps');
let sheep = "...";
if (count < 1) { 
    alert('Error');
} else {
    count > 1;
    document.write(`<p>${count + sheep}</p>`);
}
*/


//Show a user the prompt where they have to write a correct answer for 2 + 2 * 2. Show the prompt as many times until the user answers correctly.

/*let answer;
do {
    answer = prompt('What is 2+2*2');
} while (answer !=8);
alert('Correct');
*/